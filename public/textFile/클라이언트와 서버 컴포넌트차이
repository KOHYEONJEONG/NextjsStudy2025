✅서버 컴포넌트
- 서버측에서 사전 렌더링을 진행할 때 딱 한번만 실행 됨
ex)보안에 민감한 작업이나 아니면 데이터 패칭해 오는 그런 기능 등등


✅클라이언트 컴포넌트
- “use client” 해당 지시자를 page에 상단 부분에 명시
- 사전 렌더링 진행할 떄 한번, 하이드레이션 진행할 때 한번 총 2번 실행
ex) 브라우저 측에서 실행간으한 react hooks 사용 시(useEffect, useState, useRef 등등)
